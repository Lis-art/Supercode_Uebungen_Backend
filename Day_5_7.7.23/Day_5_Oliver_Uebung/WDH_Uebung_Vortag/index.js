// NAVIGIEREN und LOESCHEN

/* create() { mkdir -p "$(dirname "$1")" && touch "$1" ; }
mcd () { mkdir -p "$1" && cd "$1"; }        # mcd:          Makes new Dir and jumps inside
alias jstree='tree -I "node_modules|.git" -a .' */

// Warum werden logs in txt dateien gespeichert (oder csv) weil es sehr viel Arbeitsspeicher klauaen würde es z.B. in eine JSON zu packen - muss dann wieder peparsed gestringt usw werden
// csv hat nachteil das kein komma drin sein darf - ist in tabelle angeordnet

// Unterschied manuell anlegen oder über Terminal -> wenn man package erstellen wolen würde um es z.B. an Kollgen weiterzureichen wäre es übers Terminal korrekt